volumes:
  next-build: {}
  node-modules: {}

services:
  app:
    container_name: app
    build:
      context: .
      dockerfile: Dockerfile
    command:
      - sh
      - -c
      - |
        yarn dev
    restart: always
    tty: true
    volumes:
      - type: bind
        source: .
        target: /app
    ports:
      - '3333:3333'
      - '3334:3334'
